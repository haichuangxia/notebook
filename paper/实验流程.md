``` mermaid

flowchart LR

开始([开始])-->加载数据-->初始化模型-->加载权重-->模型编译-->定义回调-->模型训练-->保存模型([结束])

```
``` mermaid

flowchart LR

开始([开始])-->加载数据-->加载权重-->定义回调-->模型训练
开始([开始])-->初始化模型-->加载权重-->模型编译-->模型训练-->保存权重-->保存模型([结束])

```